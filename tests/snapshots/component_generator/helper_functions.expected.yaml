name: My component
description: Format and return a name.
metadata:
  annotations:
    cloud_pipelines.net: 'true'
    components new regenerate python-function-component: 'true'
    python_original_code_path: my_component.py
    python_original_code: |2

      def format_name(name):
          """Format a name for display."""
          return name.strip().title()

      def my_component(name: str) -> str:
          """Format and return a name.

          Metadata:
              version: 1.0
          """
          return format_name(name)
    version: '1.0'
    component_yaml_path: my-component.yaml
inputs:
- name: name
  type: String
outputs:
- name: Output
  type: String
implementation:
  container:
    image: python:3.12
    command:
    - sh
    - -ec
    - |
      program_path=$(mktemp)
      printf "%s" "$0" > "$program_path"
      python3 -u "$program_path" "$@"
    - |
      def _serialize_str(str_value) -> str:
          if isinstance(str_value, str):
              return str_value
          else:
              return str(str_value)

      def format_name(name):
          """Format a name for display."""
          return name.strip().title()

      def my_component(name):
          """Format and return a name.

          Metadata:
              version: 1.0
          """
          return format_name(name)

      import argparse
      _parser = argparse.ArgumentParser(prog='My component', description='Format and return a name.')
      _parser.add_argument("--name", dest="name", type=str, required=True, default=argparse.SUPPRESS)
      _parser.add_argument("----output-paths", dest="_output_paths", type=str, nargs=1)
      _parsed_args = vars(_parser.parse_args())
      _output_files = _parsed_args.pop("_output_paths", [])

      _outputs = my_component(**_parsed_args)
      _outputs = [_outputs]

      _output_serializers = [
          _serialize_str,
      ]

      import os
      for idx, output_file in enumerate(_output_files):
          try:
              os.makedirs(os.path.dirname(output_file))
          except OSError:
              pass
          with open(output_file, 'w') as f:
              f.write(_output_serializers[idx](_outputs[idx]))
    args:
    - --name
    - inputValue: name
    - '----output-paths'
    - outputPath: Output
